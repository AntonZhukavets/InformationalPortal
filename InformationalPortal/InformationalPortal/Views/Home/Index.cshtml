@model List<InformationalPortal.Models.Article>
@{
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/CSS/Portal.css" rel="stylesheet" />
    <title>Index</title>
</head>
<body>
    <div>
        @Html.ActionLink("Test","Test","Home")
        @*<label id="defaultSearchHref"></label>*@
        @*@{
            int counter = 0;
            bool exitFlag = false;
            <div id="articlesTable">
                @if (Model.Count != 0)
                {
                    <table width="100">
                        @while (!exitFlag)
                        {
                            <tr>
                                @for (int i = 0; i < 3; i++)
                                {
                                    if (counter < Model.Count)
                                    {

                                        <td width="33%">
                                            <table align="center">
                                                <tr><td align="center">Author: @Model[counter].AutherName</td></tr>
                                                <tr><td align="center">@Model[counter].Id</td></tr>
                                                <tr><td align="center">@Html.ActionLink(Model[counter].Name, "GetFullArticle", "Home", new { id = Model[counter].Id },null)</td></tr>
                                                <tr><td align="center">@Model[counter].PictureLink</td></tr>
                                            </table>
                                        </td>
                                    }
                                    else
                                    {
                                        <td width="33%"></td>
                                        exitFlag = true;
                                    }

                                    counter++;

                                }
                                @if (exitFlag)
                                {
                                    break;
                                }
                            </tr>
                        }
                    </table>
                }
            </div>
        }*@

        @Html.Partial("ArticlesTable", Model)
       
    </div>    
</body>
</html>
